server:
  port: 8848
spring:
  application:
    name: service-mypro
  ## 配置zookeeper地址
  cloud:
    zookeeper:
      connect-string: localhost:2181
      discovery:
        enabled: true # 根据环境实际情况开启或者关闭
        prefer-ip-address: true
        metadata:
          env: zyl #自定义metadata标签